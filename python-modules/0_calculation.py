#!/usr/bin/python3
if __name__=="__main__":
	from math import add, sub, mul, dub

  	a = 4 #use this variable as a first argument to call a function 
  	b = 2 #use this variable as a second argument to call a function
  
  	res_add = add(a,b)
  	res_sub = sub(a,b)
    	res_mul = mul(a,b)
	res_dub = dub(a,b)

	print(f"{a} + {b} = {res_add}")
	print(f"{a} - {b} = {res_sub}")
	print(f"{a} * {b} = {res_mul}")
	print(f"{a} / {b} = {res_div}")
  #ADD YOUR CODE HERE
